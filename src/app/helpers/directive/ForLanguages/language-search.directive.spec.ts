import { LanguageSearchDirective } from './language-search.directive';

describe('LanguageSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new LanguageSearchDirective();
    expect(directive).toBeTruthy();
  });
});
