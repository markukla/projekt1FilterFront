import { PartersSearchDirective } from './parters-search.directive';

describe('PartersSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new PartersSearchDirective();
    expect(directive).toBeTruthy();
  });
});
