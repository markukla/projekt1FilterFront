import { OrderSearchDirectiveDirective } from './order-search-directive.directive';

describe('OrderSearchDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderSearchDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
