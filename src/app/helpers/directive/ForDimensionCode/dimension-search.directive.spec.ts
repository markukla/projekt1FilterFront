import { DimensionSearchDirective } from './dimension-search.directive';

describe('DimensionSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new DimensionSearchDirective();
    expect(directive).toBeTruthy();
  });
});
