import { ProductTopSearchDirective } from './product-top-search.directive';

describe('ProductTopSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductTopSearchDirective();
    expect(directive).toBeTruthy();
  });
});
