import { ProductBottomSearchDirective } from './product-bottom-search.directive';

describe('ProductBottomSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductBottomSearchDirective();
    expect(directive).toBeTruthy();
  });
});
