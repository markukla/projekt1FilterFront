import { ProductTypeSearchDirective } from './product-type-search.directive';

describe('ProductTypeSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductTypeSearchDirective();
    expect(directive).toBeTruthy();
  });
});
