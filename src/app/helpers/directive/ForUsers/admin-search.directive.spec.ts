import { AdminSerchDirective } from './admin-serch.directive';

describe('UserSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminSerchDirective();
    expect(directive).toBeTruthy();
  });
});
