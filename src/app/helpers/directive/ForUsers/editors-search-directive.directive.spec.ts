import { EditorsSearchDirectiveDirective } from './editors-search-directive.directive';

describe('EditorsSearchDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EditorsSearchDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
